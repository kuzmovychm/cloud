{
  "name": "lambda",
  "version": "1.0.0",
  "description": "Lambda function that stores sensors data into mysql database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max",
  "license": "ISC",
  "dependencies": {
    "mysql": "^2.18.1"
  }
}
